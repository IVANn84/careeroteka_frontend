const connector = ({
    Main: {
        searchProfession,
        areaId,
        areaName,
        areas,
        professions,
    },
}) => ({
    searchProfession,
    areaId,
    areaName,
    areas,
    professions,
});

export default connector;